# Service binding
HOST=0.0.0.0
PORT=8000

# Logging
LOG_LEVEL=INFO

# Suite-wide
ZELOSBACKPLANE_URL=nats://zelosbackplane:4222
ZELOSAI_SCHEMAS_VERSION=v0.1.0

# Component-specific (add below; commit only this `.example`, never a real `.env`)
